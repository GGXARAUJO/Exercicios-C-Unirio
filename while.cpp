#include <stdio.h>

int main() {
    int contador, n;

printf("N:");
scanf("%d", &n);

    contador = 1;

    while (contador <= n)
    { 
        printf( "%d ", contador);
        contador = contador + 1;

    }


 }
