#include <stdio.h>

int main() {
    int contador, n;

printf("N:");
scanf("%d", &n);

    contador = 10;

    while (contador >= n)
    { 
        printf( "%d ", contador);
        contador = contador - 1;

    }


 }